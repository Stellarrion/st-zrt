# Cargo build artifacts (any crate's target dir)
/target/
target/

# Local/generated benchmark models.
bench/models/

# Editor / OS cruft
*~
.DS_Store
*.swp

# Local generated metadata / private automation
_types.toml
_typos.toml
bench/RESULTS.md

# Deep-dive docs are local-only (architecture, benchmark results, release checklists).
/docs/

# Local tooling / session artifacts. Agent and progress notes are never project source.
.serena/
.duet/
AGENTS.md
/AGENT.md
progress.md
progress-*.md
/docs/
